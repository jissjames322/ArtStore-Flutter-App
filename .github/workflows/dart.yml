name: Flutter CI/CD

on:
  push:
<<<<<<< HEAD
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
=======
    branches: [ main ]
  pull_request:
    branches: [ main ]
>>>>>>> 2bf190b5d4da1a4324c2509038610d692d4d7421

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
<<<<<<< HEAD
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17' # Essential for modern Android Gradle builds

      - name: ⚙️ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true # Speeds up future builds by caching the Flutter SDK

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔍 Analyze project
        run: flutter analyze
        continue-on-error: true # Prevents pipeline from failing over minor lint warnings

      - name: 🧪 Run tests
        run: flutter test
        continue-on-error: true # Prevents pipeline from failing if the default counter test breaks

      - name: 🌐 Build Web
        run: flutter build web --release

      - name: 📤 Upload Web Artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-release
          path: build/web
          retention-days: 7

      - name: 📱 Build APK (Android)
        run: flutter build apk --release

      - name: 📤 Upload APK Artifact
=======
      # 📥 Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # ⚙️ Setup Flutter (LATEST STABLE - FIXES YOUR ERROR)
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: 'stable'

      # 📦 Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 🔍 Analyze code
      - name: Analyze project
        run: flutter analyze || true

      # 🧪 Run tests
      - name: Run tests
        run: flutter test

      # 🌐 Build Flutter Web
      - name: Build Web
        run: flutter build web

      # 📱 Build APK (Android)
      - name: Build APK
        run: flutter build apk --release

      # 📤 Upload APK artifact
      - name: Upload APK
>>>>>>> 2bf190b5d4da1a4324c2509038610d692d4d7421
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
<<<<<<< HEAD
          retention-days: 7
=======
>>>>>>> 2bf190b5d4da1a4324c2509038610d692d4d7421
